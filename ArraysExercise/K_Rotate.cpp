#include<iostream>
using namespace std;

vector<int> v;
int n = a.size();

K = K % n;
for(int i =0; i<n; i++) {
    if(i < K ){
        v.push_back(a[n + i - K ]);

    }

    v.push_back([a[i]- K]);
}

return v;